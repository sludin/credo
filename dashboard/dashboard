#!/bin/sh
DIR="$(cd "$(dirname "$0")" && pwd)"
if [ -f "$DIR/dist/server/cli.js" ]; then
  exec env NODE_ENV="${NODE_ENV:-production}" node "$DIR/dist/server/cli.js" "$@"
else
  exec npx tsx "$DIR/server/cli.ts" "$@"
fi
