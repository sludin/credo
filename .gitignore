# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Scratch / notes (not tracked)
SCRATCHPAD
FLOW

# Test certs (generated artifacts)
test_certs/

bootstrap/
backup/
.env.disabled
ca-vars.env

# Visual companion brainstorm sessions
.superpowers/

# Git worktrees
.worktrees/

# Top-level install/deploy configs (contain hostnames — use examples/.install.json.example as template)
.deploy.json
.deploy*
.install.json
.install-local.json
.install-remote.json

# Bootstrap answers (operator-specific, never committed)
bootstrap.json

# TLS certificates (managed outside git)
cert.pem
privkey.pem
*.pem~

*~

.deploy-remote.json
.deploy-rsync-include

node_modules

ceremony/ca/
scripts/ceremony/ca/

# PKI ceremony output and derived trust anchor (contain private keys; operator-local)
ca/
credo-catrust.pem
.scratch/
target/
lib/credo-lib-0.1.0.tgz
deprecated/
.claude/
.scratch/



localdeploy/
TODO.md
