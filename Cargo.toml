[workspace]
members  = ["credo-lib", "corgi", "vigil", "shepherd", "credo-test", "tools/gen-fixtures"]
resolver = "2"

[profile.release]
strip         = true
opt-level     = "s"
lto           = true
codegen-units = 1
